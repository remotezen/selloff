<%# here is the username %>
<% unless (@bidders.blank? && !user_signed_in?) %>
	<small>
		bidders
	</small>
			<%= render partial: 'shared/bidding_user', collection: @bidders , as: :bid || "no bidders"%>
	<% end %>

